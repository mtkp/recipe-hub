$('#directions .new').
  html("<%= escape_javascript render('form', recipe: @recipe) %>").
    append("<%= escape_javascript render('shared/cancel_btn') %>");
$('.new .cancel').click( function() {
  $(this).parent().
    html("<%= escape_javascript render('shared/new_btn', item: :direction) %>");
});
