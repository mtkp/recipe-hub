<li>
  <%= "#{ingredient.magnitude} #{ingredient.units} #{ingredient.food}" %>
  <%= link_to 'Edit', edit_recipe_ingredient_path(@recipe, ingredient) %>
  <%= link_to 'Destroy', recipe_ingredient_path(@recipe, ingredient), method: :delete, data: { confirm: 'Are you sure?' } %>
</li>