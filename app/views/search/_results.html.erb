<% unless params[:search_terms].empty? %>
  Found <%= pluralize(@found_items.count, "match") %> for
  <%= params[:search_terms] %>
  <br /><br />
  <%= render('shared/list', items: @found_items) if @found_items %>
<% end %>
