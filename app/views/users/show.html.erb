<% if current_user == @user %>
  <h3>Welcome. This is your profile page.</h3>
  <p>Your username: <%= @user.username %></p>
  <p>Your email: <%= @user.email %></p>
  <p><%= link_to "View your recipes", user_recipes_path(@user) %></p>
  <p><%= link_to "View your stars", user_stars_path(@user) %></p>
  <p><%= link_to "Edit your account", edit_user_registration_path %></p>
<% else %>
  <h3>You are viewing the profile page for <%= @user.username %></h3>
  <p><%= link_to "View #{@user.username}'s recipes", user_recipes_path(@user) %></p>
  <p><%= link_to "View #{@user.username}'s stars", user_stars_path(@user) %></p>
<% end %>
